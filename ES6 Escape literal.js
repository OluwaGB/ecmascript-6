let a = 8;
let b = 34;
let msg = `The sum\`s is ${a+b}`;
console.log(msg);
/*The backslash (\) is used to escape string
from back tick,simgle quote or double quote,
it makes javascript treat the backtick encounter after backlash as part of string to be printed byt
not as the quote wrapping the string*/
